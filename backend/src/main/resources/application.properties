# ===============================
# DataSource Configuration
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/cabinetplusdb
spring.datasource.username=postgres
spring.datasource.password=newStrongPassword
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# Hibernate / JPA
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# ===============================
# JWT Configuration
# ===============================
# Strong 256-bit+ Base64 key for HMAC-SHA256
jwt.secret=3zQnXLz+v0Wm+9j8fH9Sv+rb3VbA9M/cD4xPTfFhM7Y=
jwt.access.expiration-ms=600000           
jwt.refresh.expiration-ms=31536000000     

# ===============================
# Mail Configuration (Gmail SMTP)
# ===============================
spring.mail.properties.mail.debug=true

spring.mail.host=smtp.gmail.com
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=cabinetplusofficiel@gmail.com
spring.mail.password=${MAIL_PASSWORD:}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# ===============================
# Twilio Configuration (SMS)
# ===============================
twilio.account.sid=${TWILIO_SID:}
twilio.auth.token=${TWILIO_TOKEN:}
twilio.phone.number=+14059373429


spring.profiles.active=dev